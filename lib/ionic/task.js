var fs = require('fs');

var IonicTask = function() { };

IonicTask.prototype = {
  IONIC_DASH: 'http://virt/',
  IONIC_COOKIES: 'ionic.cookies',
  run: function(ionic) {
  }
};

exports.IonicTask = IonicTask;