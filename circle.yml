machine:
  node:
    version: v6.1.0

dependencies:
  post:
    - ./node_modules/.bin/lerna run lint
    - ./node_modules/.bin/lerna run build

test:
  override:
    - ./node_modules/.bin/lerna run test
